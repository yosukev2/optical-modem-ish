# TEMP_GH PR Draft

用途:
- gh接続できない時に、AIがPR草案と登録コマンドを書き出す出力ファイル。
- 人間は `PowerShell Command` をコピペしてPR作成する。

## Status
- mode: draft
- reason:
- pr_url:

## Title
(skill9_pr_submit が生成)

## Body File
- (skill9_pr_submit が生成)

## Body Preview
```markdown
(skill9_pr_submit が生成)
```

## PowerShell Command
```powershell
# skill9_pr_submit 実行後にここへ出力される
```

## Bash Command
```bash
# skill9_pr_submit 実行後にここへ出力される
```
